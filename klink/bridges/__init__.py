"""Editor bridges: thin adapters that connect external layout editors to
klink's editor-agnostic core. Each bridge ships MECHANISM only (transport,
conversion, harvesting, verification harnesses); device/process data stays
example-owned per the klink purity doctrine."""

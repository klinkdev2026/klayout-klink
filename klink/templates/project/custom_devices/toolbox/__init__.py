"""toolbox — YOUR reusable klink tools. This file is the index.

Anything here has graduated from a run: it is parameterized, verified,
and expected to be imported again. Keep this docstring's export list
current — it is how the next agent (and you) discover what already
exists instead of rewriting it.

Exports:
    (none yet — promote your first tool from a run)

Graduation checklist for a run's code moving in here:
- takes its process facts as arguments (pdk.py stays the only home
  for process data; nothing hardcoded here),
- has a docstring saying what it draws/computes and its parameters,
- was verified in the run it came from (geometry/LVS evidence in that
  run's notes.md),
- is exported above.
"""
